

export const createSoybean = (seedType, plantHeight, plantOutput) => {

    // let seedId = seedId
    const plant = {
        type: seedType,
        height: plantHeight,
        output: plantOutput
    };
    // plant++;

    return plant
}